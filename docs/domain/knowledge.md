# Dominio: Knowledge Base y RAG

El **Knowledge** es el cerebro del asistente. Está compuesto por `KnowledgeSource` (el documento que sube el dueño) y `DocumentChunk` (los miles de fragmentos procesados por IA).
